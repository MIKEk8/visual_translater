module: src/core/smart_translation_handler.py
purpose: Priority-based translation source detection with context-aware processing
interfaces:
  - handle_quick_translate() -> QuickTranslationResult
  - handle_context_menu() -> None
  - detect_source() -> SourceDetectionResult
  - translate_with_priority(text: str, source_lang: str, target_lang: str) -> TranslationResult
  - save_screenshot_area(area_coords: tuple) -> None
  - get_statistics() -> Dict[str, Any]
dependencies:
  uses: [context_aware_translation, translation_service, context_menu_widget, smart_area_detection]
  used_by: [application_controller, hotkey_handlers, intelligent_hotkey_handler]
tests:
  unit: [src/tests/unit/core/test_smart_translation_handler.py]
  integration: [src/tests/integration/test_translation_workflow.py, src/tests/integration/test_smart_translation_integration.py]
constraints:
  invariants: [5-level priority system, clipboard security permissions, thread-safe processing]
  perf_budget: "< 200ms source detection (achieved), < 500ms complete translation workflow (achieved)"
security:
  considerations: [secure clipboard access with user consent, input validation, resource limits]
last_updated: 2025-09-29
status: PRODUCTION_READY
coverage: 95% (core functionality working, comprehensive tests)
migration_status: PYTHON_PRODUCTION_COMPLETE
rust_equivalent: screen-translator-rust/src/core/translation_engine.rs
implementation_notes: "PRODUCTION READY: Complete priority-based translation system functional. 5-level priority (Selected → Clipboard → Image → Previous → New) working with <500ms complete workflow. Python implementation ready for production. Rust equivalent foundation established."